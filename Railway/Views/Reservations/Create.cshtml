@using Railway.Core.Entities;
@model Railway.Core.Entities.Reservation

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Reservation</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div class="text-danger"></div>
            <div class="form-group">
                <label class="control-label"></label>
                <select name="PassagerId" asp-for="Passager"   asp-items="@(ViewBag.Passagers as SelectList)"></select>
               @* <label class="control-label"></label>*@
               
                    @*<input type="hidden" asp-for="Passager" value="@(new List<Exemplaire>())" />*@
              
            </div>
            <div class="form-group">
               
                <select name="ExemplaireId">
                   
                    @foreach (Exemplaire ex in ViewBag.Exemplaires)
                    {
                        <option value="@(ex.Id)">@( ex.Buillet.Titre)</option>
                    }
                </select>
            </div>
            <div class="form-group">
                <label asp-for="DateAller" class="control-label"></label>
                <input asp-for="DateAller" class="form-control" />
                <span asp-validation-for="DateAller" class="text-danger"></span>
            </div>@* <div class="form-group">
            <label asp-for="DateModification" class="control-label"></label>
            <input asp-for="DateModification" class="form-control" />
            <span asp-validation-for="DateModification" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <label asp-for="DateRetour" class="control-label"></label>
                <input asp-for="DateRetour" class="form-control" />
                <span asp-validation-for="DateRetour" class="text-danger"></span>
            </div>
            
           
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
